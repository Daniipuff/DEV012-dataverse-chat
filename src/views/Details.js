export const Detail = () =>{
    const li = document.querySelector('.listas');
    li.appendChild(lista);

    const tarjetas = document.createElement('dl');
    tarjetas.setAttribute('itemscope', '');
    tarjetas.setAttribute('itemtype','Detalle de personajes');
}